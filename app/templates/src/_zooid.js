import classNames from 'classnames'
import React from 'react'

import styles from './<%= zooidname %>.css'

const <%= zooidname %> = () => {
  return <span>
    <%= zooidname %>
  </span>
}

export default <%= zooidname %>
